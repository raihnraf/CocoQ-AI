---
phase: 02-flask-api-backend
plan: 03
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-02
files_modified:
  - app/__init__.py
  - app/main/__init__.py
  - app/main/routes.py
autonomous: true
requirements: []

must_haves:
  truths:
    - "Flask app starts with create_app() factory pattern"
    - "API blueprint registered at /api prefix"
    - "Main blueprint registered at / prefix with page routes"
    - "Model loaded once at app startup, stored on app config"
  artifacts:
    - path: "app/__init__.py"
      provides: "Application factory: create_app() function"
      exports: ["create_app"]
    - path: "app/main/__init__.py"
      provides: "Main blueprint registration"
      exports: ["main_bp"]
    - path: "app/main/routes.py"
      provides: "Page route handlers for dashboard, predict form, history"
      exports: []
  key_links:
    - from: "app/__init__.py"
      to: "app/ml/predict.py"
      via: "load_model() called during create_app"
      pattern: "from app\\.ml\\.predict import load_model"
    - from: "app/__init__.py"
      to: "app/api/__init__.py"
      via: "register_blueprint(api_bp)"
      pattern: "from app\\.api import api_bp"
    - from: "app/__init__.py"
      to: "app/db.py"
      via: "init_app(app) called during create_app"
      pattern: "from app\\.db import init_app"
---

<objective>
Wire everything together with Flask application factory pattern and create main blueprint page routes.

Purpose: Provide the create_app() entry point that loads the model, initializes the database, and registers both blueprints. Add basic page routes for the Phase 3 dashboard to build upon.
Output: app/__init__.py (create_app factory), app/main/__init__.py (main blueprint), app/main/routes.py (page routes)
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-flask-api-backend/02-01-PLAN.md
@.planning/phases/02-flask-api-backend/02-02-PLAN.md
@.planning/STATE.md

<interfaces>
From Plan 01 (app/db.py):
```python
def init_app(app)  # Initializes DB, registers teardown
```

From Plan 01 (app/ml/predict.py):
```python
def load_model(model_path='models/quality_model.pkl') -> Pipeline
```

From Plan 02 (app/api/__init__.py):
```python
api_bp = Blueprint('api', __name__, url_prefix='/api')
```

Existing files to modify:
- app/__init__.py (currently empty — will contain create_app)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create application factory with create_app() (API-04 wiring)</name>
  <files>app/__init__.py</files>
  <action>Replace the empty app/__init__.py with:
1. `create_app(config=None)` function that:
   - Creates Flask app: `app = Flask(__name__)`
   - Sets default config: `app.config['SECRET_KEY'] = 'dev'` (demo project, not production)
   - Sets model path: `app.config['MODEL_PATH'] = os.path.join(app.root_path, '..', 'models', 'quality_model.pkl')`
   - Merges any passed config dict into app.config
   - Loads model at startup: `pipeline = load_model(app.config['MODEL_PATH'])` and stores on `app.config['MODEL'] = pipeline`
   - If model loading fails: print error and exit with code 1 (fail fast — app is useless without model)
   - Calls `init_app(app)` from app.db to initialize database
   - Registers blueprints:
     - `from app.api import api_bp` → `app.register_blueprint(api_bp)`
     - `from app.main import main_bp` → `app.register_blueprint(main_bp)`
   - Returns app
2. Add `if __name__ == '__main__': app = create_app(); app.run(debug=True, port=5000)` block for direct execution
3. Use `os.path` to resolve paths relative to app directory, not hardcoded absolute paths
</action>
  <verify>
    <automated>python -c "from app import create_app; app = create_app(); assert app.config.get('MODEL') is not None, 'Model not loaded'; assert 'api' in {bp.name for bp in app.blueprints.values()}, 'API blueprint not registered'; assert 'main' in {bp.name for bp in app.blueprints.values()}, 'Main blueprint not registered'; print('App factory OK')"</automated>
  </verify>
  <done>create_app() loads model, initializes DB, registers both blueprints, returns configured Flask app; direct execution runs on port 5000</done>
</task>

<task type="auto">
  <name>Task 2: Create main blueprint with page routes (foundation for Phase 3)</name>
  <files>app/main/__init__.py, app/main/routes.py</files>
  <action>Create app/main/__init__.py:
1. `main_bp = Blueprint('main', __name__)`
2. Import routes after blueprint creation

Create app/main/routes.py with three page route handlers:
1. `@main_bp.route('/')` → dashboard():
   - Returns `render_template('dashboard.html')` — placeholder template for Phase 3
   - For now, return a simple HTML response: `'<h1>CocoQ-AI Dashboard</h1><p>Phase 3 will build the full dashboard UI.</p>'`
2. `@main_bp.route('/predict')` → predict_page():
   - Returns `render_template('predict.html')` — placeholder template for Phase 3
   - For now, return a simple HTML response: `'<h1>New Prediction</h1><p>Phase 3 will build the prediction form.</p>'`
3. `@main_bp.route('/history')` → history_page():
   - Returns `render_template('history.html')` — placeholder template for Phase 3
   - For now, return a simple HTML response: `'<h1>Prediction History</h1><p>Phase 3 will build the history table.</p>'`

These are placeholder routes — they confirm the blueprint is wired correctly. Phase 3 will replace them with proper Jinja2 templates.
</action>
  <verify>
    <automated>python -c "from app import create_app; app = create_app(); client = app.test_client(); r = client.get('/'); assert r.status_code == 200; r2 = client.get('/predict'); assert r2.status_code == 200; r3 = client.get('/history'); assert r3.status_code == 200; print('Main routes OK')"</automated>
  </verify>
  <done>Three page routes respond with 200 at /, /predict, /history; main_bp registered; placeholder HTML responses confirm wiring</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App startup → Model file | Model file loaded at startup — if missing or corrupted, app fails fast |
| App config → Blueprint registration | Blueprint imports could fail if routes.py has syntax errors |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | Denial of Service | create_app() | mitigate | Fail fast on model load error — app never starts in broken state, clear error message indicates missing model file |
| T-02-11 | Information Exposure | create_app() SECRET_KEY | accept | Demo project only, not production — SECRET_KEY is 'dev' placeholder, documented as such |
| T-02-12 | Spoofing | main_bp routes | mitigate | Placeholder routes return static HTML only — no user input processed, no data exposure |
</threat_model>

<verification>
- app/__init__.py contains create_app() function that returns configured Flask app
- Model loaded at startup and stored on app.config['MODEL']
- Database initialized via init_app(app)
- Both api_bp and main_bp registered
- app/main/__init__.py exports main_bp
- app/main/routes.py has three route handlers at /, /predict, /history
- All three routes return 200 status code
- `python -m app` or `flask run` starts the application on port 5000
</verification>

<success_criteria>
- create_app() returns Flask app with MODEL config key containing loaded pipeline
- api_bp accessible at /api/* endpoints (verified by test client)
- main_bp accessible at /, /predict, /history (verified by test client)
- App fails with clear error if models/quality_model.pkl is missing
- Direct execution (`python app/__init__.py` or `python -m app`) starts dev server
- No circular import errors between app/__init__.py, app/api/, app/main/, app/db.py, app/ml/
</success_criteria>

<output>
After completion, create `.planning/phases/02-flask-api-backend/02-03-SUMMARY.md`
</output>
