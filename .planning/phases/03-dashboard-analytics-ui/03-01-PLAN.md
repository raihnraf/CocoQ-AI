---
phase: 03-dashboard-analytics-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/templates/base.html
  - app/static/css/style.css
  - app/main/routes.py
autonomous: true
requirements: [UI-09]
must_haves:
  truths:
    - "User sees a consistent navigation bar across all pages"
    - "Layout adapts to mobile viewport (375px+) without horizontal scroll"
    - "Chart.js 4.x is loaded via CDN and available globally"
  artifacts:
    - path: "app/templates/base.html"
      provides: "Base layout with nav, content block, CDN includes"
      min_lines: 30
    - path: "app/static/css/style.css"
      provides: "Responsive CSS grid/flexbox utilities"
      min_lines: 40
    - path: "app/main/routes.py"
      provides: "Template rendering for /, /predict, /history routes"
      contains: "render_template"
  key_links:
    - from: "app/main/routes.py"
      to: "app/templates/base.html"
      via: "render_template"
      pattern: "render_template\\("
    - from: "app/templates/base.html"
      to: "app/static/css/style.css"
      via: "url_for('static', filename='css/style.css')"
      pattern: "url_for.*static.*style\\.css"
---

<objective>
Create the foundational layout system: base Jinja2 template with navigation, responsive CSS, and wire up main routes to render templates instead of placeholder HTML strings.

Purpose: Establish the visual foundation that all subsequent pages extend. Without this, no UI pages can render consistently.
Output: base.html, style.css, updated main routes
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@app/main/routes.py
@app/__init__.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create base.html template with navigation and layout structure</name>
  <files>app/templates/base.html</files>
  <action>
Create `app/templates/base.html` as the Jinja2 base template that all pages extend. Structure:

1. **HTML5 doctype** with `<html lang="en">`
2. **`<head>` block** containing:
   - `<meta charset="UTF-8">` and `<meta name="viewport" content="width=device-width, initial-scale=1.0">`
   - `<title>{% block title %}CocoQ-AI{% endblock %}</title>`
   - Chart.js 4.x via CDN: `<script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.7/dist/chart.umd.min.js"></script>`
   - Link to static CSS: `<link rel="stylesheet" href="{{ url_for('static', filename='css/style.css') }}">`
   - `{% block head %}{% endblock %}` for page-specific head content
3. **`<body>` with:**
   - `<nav class="navbar">` containing:
     - Logo/brand: "CocoQ-AI" linking to `/`
     - Navigation links: "Dashboard" (`/`), "New Prediction" (`/predict`), "History" (`/history`)
     - Use `<a href="{{ url_for(...) }}">` for route-safe links
   - `<main class="container">` wrapping `{% block content %}{% endblock %}`
   - `<footer class="footer">` with "CocoQ-AI — Quality Prediction Demo" text
4. **`{% block scripts %}{% endblock %}` at end of body for page-specific JS

Create the `app/templates/` directory first if it doesn't exist.
  </action>
  <verify>
    <automated>test -f app/templates/base.html && grep -c "block content" app/templates/base.html && grep -c "chart.js" app/templates/base.html</automated>
  </verify>
  <done>base.html exists with nav, content block, Chart.js CDN, CSS link, and scripts block</done>
</task>

<task type="auto">
  <name>Task 2: Create responsive CSS with grid/flexbox utilities</name>
  <files>app/static/css/style.css</files>
  <action>
Create `app/static/css/style.css` with responsive styling. Create `app/static/css/` directory first.

Required CSS sections:

1. **CSS custom properties (root):**
   - `--primary: #2d6a4f` (forest green — coconut/natural theme)
   - `--primary-light: #40916c`
   - `--secondary: #d4a373` (warm brown — sugar color)
   - `--bg: #f8f9fa`
   - `--text: #212529`
   - `--text-muted: #6c757d`
   - `--border: #dee2e6`
   - `--success: #2d6a4f`
   - `--warning: #e9c46a`
   - `--danger: #e76f51`
   - `--white: #ffffff`
   - `--shadow: 0 2px 8px rgba(0,0,0,0.08)`
   - `--radius: 8px`

2. **Reset and base:**
   - `* { box-sizing: border-box; margin: 0; padding: 0; }`
   - `body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; background: var(--bg); color: var(--text); line-height: 1.6; }`

3. **Navbar (.navbar):**
   - `display: flex; align-items: center; justify-content: space-between; padding: 1rem 2rem; background: var(--primary); color: var(--white);`
   - Brand: `font-size: 1.25rem; font-weight: 700; color: var(--white); text-decoration: none;`
   - Nav links: `display: flex; gap: 1.5rem;` with `a { color: rgba(255,255,255,0.85); text-decoration: none; padding: 0.5rem 0; }` and hover state

4. **Container (.container):**
   - `max-width: 1200px; margin: 0 auto; padding: 2rem 1rem;`

5. **Card (.card):**
   - `background: var(--white); border-radius: var(--radius); box-shadow: var(--shadow); padding: 1.5rem;`

6. **Grid utilities:**
   - `.grid { display: grid; gap: 1.5rem; }`
   - `.grid-2 { grid-template-columns: repeat(2, 1fr); }`
   - `.grid-3 { grid-template-columns: repeat(3, 1fr); }`
   - `.grid-4 { grid-template-columns: repeat(4, 1fr); }`

7. **Stat card (.stat-card):** extends .card with `text-align: center;`
   - `.stat-value { font-size: 2rem; font-weight: 700; color: var(--primary); }`
   - `.stat-label { font-size: 0.875rem; color: var(--text-muted); text-transform: uppercase; letter-spacing: 0.05em; }`

8. **Form styles:**
   - `.form-group { margin-bottom: 1rem; }`
   - `label { display: block; margin-bottom: 0.25rem; font-weight: 500; font-size: 0.875rem; }`
   - `input, select { width: 100%; padding: 0.625rem 0.75rem; border: 1px solid var(--border); border-radius: var(--radius); font-size: 1rem; }`
   - `input:focus, select:focus { outline: none; border-color: var(--primary); box-shadow: 0 0 0 3px rgba(45,106,79,0.15); }`
   - `.btn { display: inline-block; padding: 0.625rem 1.5rem; background: var(--primary); color: var(--white); border: none; border-radius: var(--radius); font-size: 1rem; cursor: pointer; text-decoration: none; }`
   - `.btn:hover { background: var(--primary-light); }`
   - `.btn-secondary { background: var(--secondary); }`

9. **Responsive breakpoints:**
   - `@media (max-width: 768px)`: `.grid-2, .grid-3, .grid-4 { grid-template-columns: 1fr; }`, `.navbar { flex-direction: column; gap: 0.75rem; padding: 1rem; }`, `.container { padding: 1rem 0.75rem; }`
   - `@media (max-width: 480px)`: `.stat-value { font-size: 1.5rem; }`, `.card { padding: 1rem; }`

10. **Footer (.footer):**
    - `text-align: center; padding: 2rem 1rem; color: var(--text-muted); font-size: 0.875rem; border-top: 1px solid var(--border); margin-top: 2rem; }`

11. **Chart container:**
    - `.chart-container { position: relative; height: 40vh; min-height: 250px; }`

12. **Utility classes:**
    - `.text-center { text-align: center; }`
    - `.text-muted { color: var(--text-muted); }`
    - `.mt-1 { margin-top: 0.5rem; } .mt-2 { margin-top: 1rem; } .mt-3 { margin-top: 1.5rem; }`
    - `.mb-1 { margin-bottom: 0.5rem; } .mb-2 { margin-bottom: 1rem; } .mb-3 { margin-bottom: 1.5rem; }`
  </action>
  <verify>
    <automated>test -f app/static/css/style.css && grep -c "grid-template-columns" app/static/css/style.css && grep -c "@media" app/static/css/style.css</automated>
  </verify>
  <done>style.css exists with CSS variables, navbar, container, card, grid, form, responsive breakpoints, chart container, and utility classes</done>
</task>

<task type="auto">
  <name>Task 3: Update main routes to render templates</name>
  <files>app/main/routes.py</files>
  <action>
Update `app/main/routes.py` to use `render_template` instead of returning placeholder HTML strings.

1. Add import: `from flask import render_template`
2. Update `dashboard()` route (`/`): `return render_template('dashboard.html')`
3. Update `predict_page()` route (`/predict`): `return render_template('predict.html')`
4. Update `history_page()` route (`/history`): `return render_template('history.html')`

The template files (dashboard.html, predict.html, history.html) will be created by subsequent plans — Flask will render them once they exist.
  </action>
  <verify>
    <automated>grep -c "render_template" app/main/routes.py</automated>
  </verify>
  <done>All three main routes use render_template instead of returning HTML strings; import added</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Server (templates) | Static HTML served to browser — no untrusted input at this stage |
| CDN → Client | Chart.js loaded from jsdelivr CDN — supply chain risk (mitigated by pinned version @4.4.7) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Chart.js CDN | mitigate | Pin to exact version @4.4.7, use SRI hash if available |
| T-03-02 | Information Disclosure | CSS variables | accept | No sensitive data in CSS — styling only |
| T-03-03 | Spoofing | Navigation links | accept | Internal navigation only, no external redirects |
</threat_model>

<verification>
- base.html extends correctly with block structure
- style.css loads without errors in browser dev tools
- All three routes (/  /predict, /history) return 200 (templates may not exist yet, but route logic is correct)
- Responsive layout verified at 375px viewport width
</verification>

<success_criteria>
- base.html exists with navbar, content block, Chart.js CDN, and scripts block
- style.css exists with CSS variables, grid system, card styles, form styles, and responsive breakpoints
- main/routes.py uses render_template for all three routes
- Layout renders correctly on both desktop and mobile viewports
</success_criteria>

<output>
After completion, create `.planning/phases/03-dashboard-analytics-ui/03-01-SUMMARY.md`
</output>
